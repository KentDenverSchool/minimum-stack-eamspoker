public class MinStack<E> {
    private int size;
    private Stack stack;
    private Stack min;

    //push, if first push, automatically push to min
    //if minStack.peek is >= as the item wanting to be pushed, push to min stack
    //in pop, if min is same, pop from boh
    //no node updating --> size is instance data, no updating nodes


}
